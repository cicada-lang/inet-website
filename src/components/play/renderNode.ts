import { NodeEntry } from '@cicada-lang/inet/lib/lang/net'
import { State } from './State'

export function renderNode(
  state: State,
  nodeEntry: NodeEntry,
  position: [number, number],
) {
  //
}
